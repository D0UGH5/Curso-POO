package listas;

public class Demo {

	public static void main(String[] args) {
		/*
		 Tamanho da lista = size()
		 inserir elementos na lista: add(obj), add(int, obj)
		 remover elementos da lista: remove(obj), remove(int), removeIf(predicado)
		 encontrar posicao do elemento :indexOf(obj), lastIndexOf(obj)
		 filtrar lista com base no predicado: list<Integer> result = list.stream().filter( x -> x >4 ).collect(Collectors.toList());
		 encontrar a primeira ocorrência com base no predicado: Integer result = list.stream().filter( x -> x > 4 ).findFirst().orElse(null);
		 */

	}

}
